# B?t cache Thymeleaf cho staging & prod (b?t bu?c!)
spring.thymeleaf.cache=true

# T?t show SQL v� m?t s? log debug ? prod
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF

# DB ? staging/prod: b?n s? set qua Environment Variables (Render/Railway s? inject)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Email ? prod: n�n d�ng env var thay v� hardcode
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

# PayOS URL th?t (s? set ? env var)
payos.return-url=${PAYOS_RETURN_URL:https://springboot-e-commerce-nz7h.onrender.com/payment-success}
payos.cancel-url=${PAYOS_CANCEL_URL:https://springboot-e-commerce-nz7h.onrender.com/payment-cancel}